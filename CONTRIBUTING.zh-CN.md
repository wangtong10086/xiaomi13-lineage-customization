# 贡献指南

[English](CONTRIBUTING.md)

贡献应在 Xiaomi 13（`fuxi`）上保持范围小、可回滚、可审阅。

1. 若要新增设备、ROM、应用版本或改变行为，请先创建 Issue，不要直接放宽精确版本守卫。
2. 将只读审计与写入逻辑分离。写入必须要求 `-Apply` 或同等明确动作、指定单一目标设备、先备份受影响状态，并在版本/哈希不匹配时拒绝继续。
3. 不得使用 `pm clear`、隐式 `force-stop`、无边界禁用软件包或直接写应用私有数据库。
4. 不得提交 APK、ROM 镜像、keybox、签名材料、钱包/TSM 数据、账户标识、FCM/XMSF 注册 ID、联系人、消息正文、私有数据库、原始日志、序列号或个人应用清单。
5. 使用脱敏 fixture 与 `.example` 输入；记录验证过的精确公开构建标识、回滚结果和无关功能检查。
6. PR 前完成 PowerShell 解析、JSON 校验、shell 检查及相关确定性构建测试。

`wallet/`、`root/` 与 `push/` 下的改动需要额外审阅，因为很小的作用域或生命周期错误也可能影响安全性或可用性。
